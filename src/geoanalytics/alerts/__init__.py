"""Алерты (M5.3): триггеры поверх собранных данных и доставка уведомлений.

Слои (как в остальном проекте):
- `rules`    — чистые правила (без БД): срез состояния → список Alert. Главный предмет тестов.
- `channels` — доставка (console/log всегда + Telegram, если настроен), graceful.
- `engine`   — DB-раннер: собирает состояние, гоняет правила, дедуплицирует, шлёт.
"""
