"""Входящий Telegram-бот (Волна 5a): команды /ask /asset /portfolio /alerts.

Бот — интерактивная альтернатива дашборду поверх тех же раннеров. 5a: read-only, single-user
(отвечает только chat_id из allowlist). Мультиюзер и per-user настройки — подволны 5b/5c.
"""
